<UserControl x:Class="Animator.UI.Editors.StepListEditor"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
			 d:DesignHeight="200" d:DesignWidth="400">
	<DockPanel>
		<ListBox x:Name="stepsListBox" VerticalContentAlignment="Stretch" ItemsSource="{Binding Steps}" Margin="4">
			<ListBox.Template>
				<ControlTemplate TargetType="{x:Type ListBox}">
					<StackPanel Orientation="Horizontal" IsItemsHost="True"/>
				</ControlTemplate>
			</ListBox.Template>
			<ListBox.ItemTemplate>
				<DataTemplate>
					<DockPanel>
						<Slider x:Name="ValueSlider" Value="{Binding Path=.}" Orientation="Vertical" Maximum="100" Width="24" Margin="10,4,8,4" />
						<TextBox Text="{Binding ElementName=ValueSlider, Path=Value}" DockPanel.Dock="Bottom" HorizontalContentAlignment="Center" />
					</DockPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</DockPanel>
</UserControl>
