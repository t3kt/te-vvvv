<ListBox x:Class="Animator.UI.Panes.DocItemNavList"
		 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
		 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
		 xmlns:model="clr-namespace:Animator.Core.Model;assembly=anicore"
		 xmlns:seq="clr-namespace:Animator.Core.Model.Sequences;assembly=anicore"
		 xmlns:ses="clr-namespace:Animator.Core.Model.Sessions;assembly=anicore"
		 xmlns:io="clr-namespace:Animator.Core.IO;assembly=anicore"
		 xmlns:appCore="clr-namespace:Animator.AppCore"
		 mc:Ignorable="d" 
		 d:DesignHeight="300" d:DesignWidth="300"
		 Background="{x:Null}" HorizontalContentAlignment="Stretch">
	<ListBox.CommandBindings>
		<CommandBinding Command="appCore:AniAppCommands.ShowEditDetailCommand" Executed="Item_ShowEditDetailCommand_Executed" CanExecute="Item_ShowEditDetailCommand_CanExecute"/>
		<CommandBinding Command="ApplicationCommands.Delete" Executed="Item_DeleteCommand_Executed" CanExecute="Item_DeleteCommand_CanExecute"/>
	</ListBox.CommandBindings>
	<ListBox.ItemContainerStyle>
		<Style TargetType="{x:Type ListBoxItem}">
			<Setter Property="ContextMenu">
				<Setter.Value>
					<ContextMenu>
						<MenuItem Header="_Edit" Command="appCore:AniAppCommands.ShowEditDetailCommand" CommandParameter="{Binding}"/>
						<MenuItem Header="_Delete" Command="ApplicationCommands.Delete" CommandParameter="{Binding}" />
					</ContextMenu>
				</Setter.Value>
			</Setter>
		</Style>
	</ListBox.ItemContainerStyle>
	<ListBox.ItemTemplate>
		<DataTemplate DataType="{x:Type model:DocumentItem}">
			<Border Style="{StaticResource PaneBorderStyle}" Margin="{TemplateBinding Margin}">
				<DockPanel>
					<Image DockPanel.Dock="Left" Width="16" Height="16" Margin="0,0,4,0">
						<Image.Style>
							<Style TargetType="{x:Type Image}">
								<Style.Triggers>
									<DataTrigger Binding="{Binding Path=ItemType.Type}" Value="{x:Type seq:Sequence}">
										<Setter Property="Source" Value="{StaticResource SequencingIcon}"/>
									</DataTrigger>
									<DataTrigger Binding="{Binding Path=ItemType.Type}" Value="{x:Type ses:Session}">
										<Setter Property="Source" Value="{StaticResource SessionsIcon}"/>
									</DataTrigger>
									<DataTrigger Binding="{Binding Path=ItemType.Type}" Value="{x:Type io:Output}">
										<Setter Property="Source" Value="{StaticResource OutputIcon}"/>
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Image.Style>
					</Image>
					<TextBlock Text="{Binding Path=Name}"/>
				</DockPanel>
			</Border>
		</DataTemplate>
	</ListBox.ItemTemplate>
</ListBox>
